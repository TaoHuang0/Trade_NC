# API Documentations
## Command Line Arguments
TTo begin, download the repository and run <code>npm install</code> in the command-line. This will install each dependency needed for running the application.
